name: Test Scheduler

on:
  schedule:
    - cron: "*/5 * * * *"  # Every 5 minutes
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "UTC time: $(date -u)"
          echo "Berlin time: $(TZ=Europe/Berlin date)"

# Added comment to trigger workflow schedule activation
